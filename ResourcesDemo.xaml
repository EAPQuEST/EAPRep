<Window x:Class="WpfDemo.ResourcesDemo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDemo"
        mc:Ignorable="d"
        Title="ResourcesDemo" Height="450" Width="800">
    <!--<Window.Resources>
        <SolidColorBrush Color="Red" x:Key="RedBrush"/>
        <SolidColorBrush Color="Blue" x:Key="BlueBrush"/>


    </Window.Resources>-->
    <Grid>
        <Grid.Resources>
            <SolidColorBrush Color="Red" x:Key="RedBrush"/>
            <SolidColorBrush Color="Blue" x:Key="BlueBrush"/>
        </Grid.Resources>
        <Button Content="My Button" Background="{StaticResource RedBrush}" Width="200" Height="70"/>
        <Button Content="My Button1" BorderBrush="{StaticResource BlueBrush}" Width="200" Height="70" Margin="296,277,296,72"/>
    </Grid>
</Window>
